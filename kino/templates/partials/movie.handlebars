 
<a href="/movies">Back to all movies</a>
<div class="oneMovie">
<article class="movie">
  <h1 >{{ movie.attributes.title }}</h1>
 
  {{#if movie.attributes.image }}
  <img src="{{ movie.attributes.image.url }}">
  {{/if }}
   <div class="intro">
    {{{ markdown movie.attributes.intro }}}
  </div>
</div>
